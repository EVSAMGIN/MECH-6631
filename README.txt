This solution tracks 4 user selected targets

Tracking thread applies four separate colour masks (RGBY) using HSV filters and then merges the image

Run the solution and in the console 

-place cursor on targets and press 'C'
-press 'V' to cycle through the different masks